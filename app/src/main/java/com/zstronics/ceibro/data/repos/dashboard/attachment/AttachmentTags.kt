package com.zstronics.ceibro.data.repos.dashboard.attachment

enum class AttachmentTags(val tagValue: String) {
    Image("image"), ImageWithComment("image+comment"), File("file"), Drawing("drawing")
}
